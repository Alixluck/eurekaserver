spring:
  application:
    name: eurekaserver
#配置端口
server:
  port: 8761
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/  #把自己注册上去
    register-with-eureka: false #不让自己注册上去